# Benchcard

Read `AGENTS.md` — it is the whole harness: the loop, layout, traps, rules,
deploy, and what `.claude/` enforces. `REVIEW.md` is the review policy.
Open work is `notes/TICKETS.md`. `notes/ROADMAP.md` is the why.

One answer lives in one place. If something here disagreed with `AGENTS.md`,
there would be two answers, which is the defect that has caused more bugs in
this repo than any other. So it is not repeated here.
